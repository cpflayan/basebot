"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createAnvilTestClient = void 0;
const viem_1 = require("viem");
const actions_1 = require("viem/actions");
const viem_deal_1 = require("viem-deal");
const viem_tracer_1 = require("viem-tracer");
const fixtures_js_1 = require("./fixtures.js");
const getFunctionCalls_js_1 = require("./utils/getFunctionCalls.js");
const createAnvilTestClient = (transport, chain) => (0, viem_1.createTestClient)({
    chain,
    mode: "anvil",
    account: (0, fixtures_js_1.testAccount)(),
    transport: (0, viem_tracer_1.traced)(transport),
    cacheTime: Number.POSITIVE_INFINITY,
})
    .extend(viem_deal_1.dealActions)
    .extend(viem_tracer_1.traceActions)
    .extend(viem_1.publicActions)
    .extend(viem_1.walletActions)
    .extend((client) => {
    let automine;
    return {
        async timestamp() {
            const latestBlock = await client.getBlock();
            return latestBlock.timestamp;
        },
        async approve(args) {
            args.abi = viem_1.erc20Abi;
            args.functionName = "approve";
            // @ts-expect-error
            return this.writeContract(args);
        },
        async balanceOf({ erc20 = "0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE", owner = client.account.address, } = {}) {
            if (erc20 === "0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE")
                return client.getBalance({ address: owner });
            return client.readContract({
                address: erc20,
                abi: viem_1.erc20Abi,
                functionName: "balanceOf",
                args: [owner],
            });
        },
        async allowance({ erc20 = "0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE", owner = client.account.address, spender, }) {
            if (erc20 === "0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE")
                return viem_1.maxUint256;
            return client.readContract({
                address: erc20,
                abi: viem_1.erc20Abi,
                functionName: "allowance",
                args: [owner, spender],
            });
        },
        async maxWithdraw({ erc4626, owner = client.account.address, }) {
            return client.readContract({
                address: erc4626,
                abi: viem_1.erc4626Abi,
                functionName: "maxWithdraw",
                args: [owner],
            });
        },
        async previewMint({ erc4626, shares, }) {
            return client.readContract({
                address: erc4626,
                abi: viem_1.erc4626Abi,
                functionName: "previewMint",
                args: [shares],
            });
        },
        async convertToShares({ erc4626, assets, }) {
            return client.readContract({
                address: erc4626,
                abi: viem_1.erc4626Abi,
                functionName: "convertToShares",
                args: [assets],
            });
        },
        async convertToAssets({ erc4626, shares, }) {
            return client.readContract({
                address: erc4626,
                abi: viem_1.erc4626Abi,
                functionName: "convertToAssets",
                args: [shares],
            });
        },
        async deposit(args) {
            args.abi = viem_1.erc4626Abi;
            args.functionName = "deposit";
            // @ts-expect-error
            return this.writeContract(args);
        },
        async deployContractWait(args) {
            const hash = await client.deployContract(args);
            const receipt = await client.waitForTransactionReceipt({ hash });
            if (receipt.contractAddress == null)
                throw Error("no contract address");
            return receipt;
        },
        async writeContract(args) {
            const hash = await (0, actions_1.writeContract)(client, args);
            if ((automine ??= await client.getAutomine()))
                await client.waitForTransactionReceipt({ hash });
            return hash;
        },
        async sendTransaction(args) {
            const hash = await (0, actions_1.sendTransaction)(client, args);
            if ((automine ??= await client.getAutomine()))
                await client.waitForTransactionReceipt({ hash });
            return hash;
        },
        async sendRawTransaction(args) {
            const hash = await (0, actions_1.sendRawTransaction)(client, args);
            if ((automine ??= await client.getAutomine()))
                await client.waitForTransactionReceipt({ hash });
            return hash;
        },
        getFunctionCalls(args) {
            return (0, getFunctionCalls_js_1.getFunctionCalls)(client, args);
        },
    };
});
exports.createAnvilTestClient = createAnvilTestClient;
