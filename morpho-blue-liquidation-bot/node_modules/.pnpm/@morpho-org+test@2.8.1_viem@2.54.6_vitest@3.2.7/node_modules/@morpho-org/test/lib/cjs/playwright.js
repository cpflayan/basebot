"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.expect = exports.createViemTest = void 0;
const test_1 = require("@playwright/test");
const viem_1 = require("viem");
const anvil_js_1 = require("./anvil.js");
const client_js_1 = require("./client.js");
const createViemTest = (chain, parameters = {}) => {
    parameters.forkChainId ??= chain?.id;
    parameters.forkUrl ??= chain?.rpcUrls.default.http[0];
    parameters.autoImpersonate ??= true;
    parameters.order ??= "fifo";
    parameters.stepsTracing ??= true;
    parameters.gasPrice ??= 0n;
    parameters.blockBaseFeePerGas ??= 0n;
    return test_1.test.extend({
        // biome-ignore lint/correctness/noEmptyPattern: required by playwright at runtime
        client: async ({}, use) => {
            const { rpcUrl, stop } = await (0, anvil_js_1.spawnAnvil)(parameters);
            const client = (0, client_js_1.createAnvilTestClient)((0, viem_1.http)(rpcUrl), chain);
            // Make block timestamp 100% predictable.
            await client.setBlockTimestampInterval({ interval: 1 });
            await use(client);
            await stop();
        },
    });
};
exports.createViemTest = createViemTest;
exports.expect = test_1.test.expect.extend({
    // biome-ignore lint/complexity/useMaxParams: TODO refactor to ≤2 params
    toApproxEqual(received, expected, numDigits = 2, decimals = 18) {
        const assertionName = "toApproxEqual";
        const receivedNumber = Number((0, viem_1.formatUnits)(received, decimals));
        const expectedNumber = Number((0, viem_1.formatUnits)(expected, decimals));
        let pass;
        // biome-ignore lint/suspicious/noExplicitAny: Playwright matcher result is untyped
        let matcherResult;
        try {
            test_1.test.expect(receivedNumber).toBeCloseTo(expectedNumber, numDigits);
            pass = true;
        }
        catch (error) {
            // @ts-expect-error
            matcherResult = error.matcherResult;
            pass = false;
        }
        return {
            message: () => {
                return `${this.utils.matcherHint(assertionName, undefined, undefined, {
                    isNot: this.isNot,
                })}

Expected: ${this.utils.printExpected(expectedNumber)}
Received: ${this.utils.printReceived(receivedNumber)}

Expected precision:  ${numDigits}
Expected difference: ${this.isNot ? ">=" : "<"} ${this.utils.printExpected(10 ** -numDigits / 2)}
Received difference: ${this.utils.printReceived(Math.abs(receivedNumber - expectedNumber))}`;
            },
            pass,
            name: assertionName,
            expected,
            actual: matcherResult?.actual,
        };
    },
});
