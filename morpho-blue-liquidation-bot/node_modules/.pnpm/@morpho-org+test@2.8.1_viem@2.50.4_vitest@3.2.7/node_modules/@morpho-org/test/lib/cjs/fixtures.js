"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.testAccount = exports.randomAddress = void 0;
const viem_1 = require("viem");
const accounts_1 = require("viem/accounts");
let seed = 1;
const seededRandom = () => {
    const x = Math.sin(seed++) * 10000;
    return x - Math.floor(x);
};
const randomAddress = (chainId) => (0, viem_1.checksumAddress)(`0x${new Array(40)
    .fill(0)
    .map(() => Math.floor(seededRandom() * 16).toString(16))
    .join("")}`, chainId);
exports.randomAddress = randomAddress;
const testAccount = (addressIndex) => (0, accounts_1.mnemonicToAccount)("test test test test test test test test test test test junk", { addressIndex });
exports.testAccount = testAccount;
