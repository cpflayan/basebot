"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createViemTest = void 0;
const promises_1 = require("node:timers/promises");
const viem_1 = require("viem");
const vitest_1 = require("vitest");
const anvil_js_1 = require("./anvil.js");
const client_js_1 = require("./client.js");
// Vitest needs to serialize BigInts to JSON, so we need to add a toJSON method to BigInt.prototype.
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/BigInt#use_within_json
// @ts-expect-error
BigInt.prototype.toJSON = function () {
    return this.toString();
};
const spawnAnvilWithRetry = async (parameters) => {
    const maxAttempts = process.env.CI ? 3 : 1;
    let lastError;
    for (let attempt = 1; attempt <= maxAttempts; attempt++) {
        try {
            return await (0, anvil_js_1.spawnAnvil)(parameters);
        }
        catch (error) {
            lastError = error;
            if (attempt === maxAttempts)
                break;
            await (0, promises_1.setTimeout)(attempt * 1_000);
        }
    }
    throw lastError;
};
const createViemTest = (chain, parameters = {}) => {
    parameters.forkChainId ??= chain?.id;
    parameters.forkUrl ??= chain?.rpcUrls.default.http[0];
    parameters.autoImpersonate ??= true;
    parameters.order ??= "fifo";
    parameters.stepsTracing ??= true;
    parameters.pruneHistory ??= true;
    parameters.retries ??= process.env.CI ? 10 : undefined;
    parameters.forkRetryBackoff ??= process.env.CI ? 500 : undefined;
    parameters.gasPrice ??= 0n;
    parameters.blockBaseFeePerGas ??= 0n;
    return vitest_1.test.extend({
        // biome-ignore lint/correctness/noEmptyPattern: required by vitest at runtime
        client: async ({}, use) => {
            const { rpcUrl, stop } = await spawnAnvilWithRetry(parameters);
            try {
                const client = (0, client_js_1.createAnvilTestClient)((0, viem_1.http)(rpcUrl, {
                    fetchOptions: {
                        cache: "force-cache",
                    },
                    timeout: 30_000,
                }), chain);
                // Make block timestamp 100% predictable.
                await client.setBlockTimestampInterval({ interval: 1 });
                // Remove code from contract
                // cf. https://eips.ethereum.org/EIPS/eip-7702
                const code = await client.getCode({ address: client.account.address });
                if (code != null) {
                    const auth = await client.signAuthorization({
                        account: client.account,
                        contractAddress: viem_1.zeroAddress,
                        executor: "self",
                    });
                    await client
                        .sendTransaction({
                        authorizationList: [auth],
                        to: client.account.address,
                        data: "0x",
                        account: client.account,
                    })
                        .catch(async (e) => {
                        if (e.cause.details ===
                            "EIP-7702 authorization lists are not supported before the Prague hardfork")
                            return;
                        throw e;
                    });
                }
                await use(client);
            }
            finally {
                stop();
            }
        },
    });
};
exports.createViemTest = createViemTest;
