/**
 * Humanizes an array of strings into a comma-separated list with an "or" before the last item.
 * For example, ["a", "b", "c"] becomes "a, b or c".
 *
 * @param items Array of strings
 * @returns Humanized union
 * @example
 * ```ts
 * import { formatUnion } from "@morpho-org/morpho-ts";
 *
 * const value = formatUnion(["supply", "borrow", "withdraw"]);
 * // "supply, borrow or withdraw"
 * ```
 */
export declare function formatUnion(items: string[]): string;
/**
 * Humanizes an array of strings into a comma-separated list with an "and" before the last item.
 * For example, ["a", "b", "c"] becomes "a, b and c".
 *
 * @param items Array of strings
 * @returns Humanized enumeration
 * @example
 * ```ts
 * import { formatEnumeration } from "@morpho-org/morpho-ts";
 *
 * const value = formatEnumeration(["supply", "borrow", "withdraw"]);
 * // "supply, borrow and withdraw"
 * ```
 */
export declare function formatEnumeration(items: string[]): string;
