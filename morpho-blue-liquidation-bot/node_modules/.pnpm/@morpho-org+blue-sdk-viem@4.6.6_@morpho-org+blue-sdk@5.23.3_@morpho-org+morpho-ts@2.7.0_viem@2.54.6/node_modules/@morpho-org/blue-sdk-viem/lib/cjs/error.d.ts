import type { Address, ChainId } from "@morpho-org/blue-sdk";
/** Thrown when a permit domain targets another chain; consumers should not sign it. */
export declare class InvalidPermitDomainChainIdError extends Error {
    readonly token: Address;
    readonly expectedChainId: ChainId;
    readonly domainChainId: number | undefined;
    constructor(token: Address, expectedChainId: ChainId, domainChainId: number | undefined);
}
/** Thrown when a permit domain targets another token; consumers should not sign it. */
export declare class InvalidPermitDomainVerifyingContractError extends Error {
    readonly token: Address;
    readonly domainVerifyingContract: Address | undefined;
    constructor(token: Address, domainVerifyingContract: Address | undefined);
}
/** Thrown when a permit domain advertises unsupported EIP-5267 extensions. */
export declare class UnsupportedPermitDomainExtensionsError extends Error {
    readonly token: Address;
    readonly extensions: readonly bigint[];
    constructor(token: Address, extensions: readonly bigint[]);
}
/**
 * Checks if an error is a contract revert with the "UnknownOfFactory" error name.
 * Used to propagate factory validation errors instead of falling back to multicall.
 */
export declare function isUnknownOfFactoryError(error: unknown): boolean;
/**
 * Checks if an error is a contract revert with the "UnsupportedVaultV2Adapter"
 * error name, returning the offending adapter address if so. Used to propagate
 * adapter validation errors from the deployless `GetVaultV2.query` to the
 * caller as a typed {@link UnsupportedVaultV2AdapterError}.
 */
export declare function getUnsupportedVaultV2Adapter(error: unknown): Address | null;
